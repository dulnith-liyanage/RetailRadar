[theme]
base = "dark"
primaryColor = "#88C0D0"            # Nord Frost — buttons, active elements, links
backgroundColor = "#2E3440"         # Nord Polar Night — main app background
secondaryBackgroundColor = "#3B4252" # Nord Polar Night (lighter) — sidebar, containers
textColor = "#ECEFF4"               # Nord Snow Storm — main text
font = "sans serif"